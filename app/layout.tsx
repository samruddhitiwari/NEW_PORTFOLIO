import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Samruddhi Tiwari - Full-Stack Developer & AI Enthusiast",
  description: "Portfolio of Samruddhi Tiwari, a full-stack developer specializing in AI-powered SaaS tools and web applications.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}

